<style>
    :root {
        --mnb-blue: #0033a0;
        --soft-bg: #f8fafc;
        --charcoal: #1e293b;
    }

    body {
        background-color: var(--soft-bg);
        color: var(--charcoal);
        font-family: 'Inter', system-ui, -apple-system, sans-serif;
        line-height: 1.5;
    }

    .text-mnb { color: var(--mnb-blue); }
    .btn-mnb { background-color: var(--mnb-blue); color: white; border: none; transition: 0.2s; }
    .btn-mnb:hover { background-color: #002277; color: white; transform: translateY(-2px); }

    .white-glass {
        background: rgba(255, 255, 255, 0.9);
        border: 1px solid rgba(226, 232, 240, 1);
        border-radius: 12px;
    }

    .stat-card {
        background: white;
        transition: 0.3s;
    }
    .stat-card:hover { transform: scale(1.02); }

    .custom-input {
        border: 1px solid #e2e8f0;
        border-radius: 8px;
        padding: 0.6rem;
        background-color: white;
        color: var(--charcoal);
    }
    .custom-input:focus {
        border-color: var(--mnb-blue);
        box-shadow: 0 0 0 4px rgba(0, 51, 160, 0.1);
    }

    .finding-container {
        background-color: #f1f5f9;
        max-height: 150px;
        overflow-y: auto;
    }

    .drop-box {
        border: 2px dashed #cbd5e1;
        padding: 10px;
        border-radius: 10px;
        background-color: #f8fafc;
        transition: 0.2s;
    }
    .drop-box:hover { border-color: var(--mnb-blue); background-color: white; }

    .table thead th {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: #64748b;
        border-bottom: 2px solid #f1f5f9;
    }
    .table td { font-size: 0.9rem; color: #334155; }

    .badge { padding: 0.5em 1em; border-radius: 20px; font-weight: 600; }
</style>